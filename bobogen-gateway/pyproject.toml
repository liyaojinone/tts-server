[project]
name = "bobogen-gateway"
version = "0.1.0"
description = "Unified BoboGen Gateway for CosyVoice, F5-TTS, and GPT-SoVITS"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "httpx",
  "pydantic",
  "pyyaml",
  "uvicorn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
