[project]
name = "bobogen-service-kit"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["fastapi", "httpx", "pydantic", "python-multipart"]

[tool.pytest.ini_options]
testpaths = ["tests"]
