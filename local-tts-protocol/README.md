# local-tts-protocol

Shared protocol models for local TTS services.

## Purpose

This package defines the common HTTP contract shared by model-specific services:

- voice listing
- synthesize request/response schema
- common parameter fields
- error response shape

## Install

Install into each model service environment:

```bash
pip install -e E:\AiModel\tts\local-tts-protocol
```
